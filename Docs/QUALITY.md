# 公开测试质量记录

## 0.3.1 · 播放与测量

| 复现路径 | 修正 | 验证 |
| --- | --- | --- |
| 恢复队列后暂停，播放器仍写“正在播放” | 标题采用统一状态快照 | 实际播放器截图、界面回归 |
| 准备歌曲时发生音频中断 | 中断期间禁止准备回调恢复播放；暂停优先于系统恢复建议 | 本地 WAV 驱动 AVQueuePlayer，通知注入 |
| 连续切歌或准备期间多次拖动进度 | 每次加载和跳转有独立版本，旧完成回调不覆盖新意图 | 原生时序回归 |
| 大字体使歌词、队列和播放按钮滑出首屏 | 元数据滚动，控制区固定，封面自适应缩小 | 最大辅助字号界面用例 |
| 出现卡顿却没有可用的连接式性能录制 | 系统性能标记与本机白名单诊断；接收 MetricKit 汇总 | 保留期限、体积上限、字段白名单测试 |

自动验证：55 项核心、19 项原生、12 项界面测试通过。原生播放使用独立无声 WAV，音频中断使用系统通知注入；不代表已经完成真实来电和耳机验收。界面运行于 iOS 26.5 模拟器。

发布优化真机构建成功。设备安装时连接服务报告设备不可用，覆盖安装与本轮真机体验仍待确认。

## 测量边界

诊断只包含日期、事件类别、成功／失败／取消及毫秒耗时。事件不携带账户、请求地址、搜索词、曲目或凭据。最多保留 7 天或 5 MB，设置中预览和导出，默认不上传。

启动计时目前表示应用初始化，缓存和搜索计时表示数据层工作；不能将它们当作完整页面可操作或屏幕绘制耗时。播放准备到实际播放状态的计时也不等于物理扬声器延迟。MetricKit 的系统诊断由系统延后交付。

30 次真机数据层基线已在 0.3.0 测量：一万首曲目的内存读取 P95 0.05 ms、磁盘首批读取 P95 21.26 ms。设备为 iPhone 17、iOS 26.6.1，PersonalRelease。此数据不替代新版本或整页性能验收。

## 仍需实际设备验收

- iOS 18、小屏真机、VoiceOver 人工操作、提高对比度、减少动态效果。
- 使用原有数据覆盖升级后的收藏、队列和账号一致性。
- 真机冷启动、整页内容可操作、滚动及转场卡顿，每项至少 30 次。
- 连续播放、锁屏、网络切换、耳机断开、真实来电和 AirPlay。
- 用户自选厂商的真实模型质量；协议固定响应测试不能代替质量评估。

这些项目完成前保持开发版本，不标记为 0.4.0 公开测试验收通过。
